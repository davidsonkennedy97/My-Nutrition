package com.example.nutriplan.ui.theme

import androidx.compose.ui.graphics.Color

// Cor principal (verde solicitado)
val PrimaryGreen = Color(0xFF7F7952)

// Tema claro
val CreamBackground = Color(0xFFF6F1E3)
val OnBackgroundLight = Color(0xFFF6F1E3)

// Tema escuro (refinado: fundo escuro, mas não preto)
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1A1A1A)
val OnBackgroundDark = Color(0xFFEAEAEA)
