package com.example.nutriplan.core

object InMemoryAuth {
    var hasRegistration: Boolean = true  // Já tem cadastro por padrão
    var registeredPhoneDigits: String = "1"  // Seu telefone
    var registeredPassword: String = "1"  // Sua senha
}
